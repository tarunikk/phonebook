{
  "name": "pb_backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "@powershell Remove-Item -Recurse -Force dist && cd ../pb_frontend && npm run build && @powershell Copy-Item dist -Recurse ../phonebook",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tarunikk/phonebook.git"
  },
  "author": "Taru Nikkanen",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/tarunikk/phonebook/issues"
  },
  "homepage": "https://github.com/tarunikk/phonebook#readme",
  "description": "",
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "mongoose": "^8.19.2",
    "morgan": "^1.10.1"
  }
}
